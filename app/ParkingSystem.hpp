#ifndef PARKINGSYSTEM_HPP
#define PARKINGSYSTEM_HPP

// Shared business backend. GUI and TUI both include this header; the
// implementation currently lives in the compatibility _tui header so there is
// no second ParkingSystem class or divergent data format.
#include "ParkingSystem_tui.hpp"

#endif // PARKINGSYSTEM_HPP
