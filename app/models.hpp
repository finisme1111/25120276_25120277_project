#ifndef MODELS_HPP
#define MODELS_HPP

// Shared data model backend. The compatibility header keeps the historical
// _tui include path working while GUI and TUI use the same definitions.
#include "models_tui.hpp"

#endif // MODELS_HPP
